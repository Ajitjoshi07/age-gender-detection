CREATE DATABASE IF NOT EXISTS detections_db;
USE detections_db;
CREATE TABLE IF NOT EXISTS faces (
    id INT AUTO_INCREMENT PRIMARY KEY,
    age INT,
    gender VARCHAR(10),
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
);